import InnerWrapper from '@components/InnerWrapper';

export default function Projects() {
  return (
    <>
      <section>
        <InnerWrapper>Projects</InnerWrapper>
      </section>
    </>
  );
}
