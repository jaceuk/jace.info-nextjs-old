import InnerWrapper from '@components/InnerWrapper';
import styles from './Contact.module.scss';

export default function Contact() {
  return (
    <>
      <section>
        <InnerWrapper>
          <h1 className="h2">Contact me</h1>
        </InnerWrapper>
      </section>

      <section>
        <InnerWrapper>Contact</InnerWrapper>
      </section>
    </>
  );
}
